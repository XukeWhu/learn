package top;

import top.top124.TreeNode;

public class top437 {
	int count=0;
	public int pathSum(TreeNode root, int targetSum) {
		if(root==null)return 0;
		count=0;
		find(root,targetSum);
    }
}
